---
layout: ../../layouts/main.astro
title: Éditions précédentes
description: Éditions précédentes
---
import PreviousSponsorsLogosSection from '../../components/PreviousSponsorsLogosSection.astro';
import organisation from '../../data/organisation.json';
import edition from '../../data/edition.json';

export const editions = organisation.past.editions
  .toReversed()
  .filter((year) => new Date(edition.start_date).getFullYear() !== year);

{editions.map((year) => (<PreviousSponsorsLogosSection year={year} />))}
